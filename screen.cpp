#include "screen.h"

void main_menu::drawScreen()
{
	ClearBackground(BLACK);
	DrawText("MAIN MENU", GetScreenWidth() / 2, GetScreenHeight() / 2, 30, GREEN);
}

void main_menu::updateScreen()
{

}

void settings::drawScreen()
{
	ClearBackground(BLACK);
	DrawText("MAIN MENU", GetScreenWidth() / 2, GetScreenHeight() / 2, 30, GREEN);
}

void settings::updateScreen()
{

}
